--[[
  Extracted from: ServerScriptService.Services.CmdrService.Commands.CustomCommands.spawnroadbrainrotServer
  Class: ModuleScript
  Source file: rickdev.rbxlx
]]

local MessagingService = game:GetService("MessagingService") local ServerScriptService = game:GetService("ServerScriptService") local ReplicatedStorage = game:GetService("ReplicatedStorage") local HttpService = game:GetService("HttpService") local Animals = require(ReplicatedStorage.Datas.Animals) local Mutations = require(ReplicatedStorage.Datas.Mutations) local Traits = require(ReplicatedStorage.Datas.Traits) local function spawnRoadAnimal(animalName, mutation, traits) local animalData = nil local animalIndex = animalName animalData = Animals[animalIndex] if not animalData then return false, "Animal '" .. animalName .. "' not found" end if mutation and mutation ~= "" and mutation ~= "Default" then if not Mutations[mutation] then return false, "Mutation '" .. mutation .. "' not found" end else mutation = nil end local validatedTraits = nil if traits and traits ~= "" then local success, decodedTraits = pcall(function() return HttpService:JSONDecode(traits) end) if not success or type(decodedTraits) ~= "table" then return false, "Invalid traits format. Use JSON array like '[\"Taco\", \"Fireworks\"]'" end for _, trait in ipairs(decodedTraits) do if not Traits[trait] then return false, "Trait '" .. trait .. "' not found" end end validatedTraits = decodedTraits end local roadService = _G.RoadAnimalService if not roadService or not roadService.Spawner then return false, "RoadAnimalService not available" end local originalCallback = roadService.Spawner.AnimalSpawnedCallback if validatedTraits and #validatedTraits > 0 then print("ð§ DEBUG: Setting up trait callback for traits:", HttpService:JSONEncode(validatedTraits)) roadService.Spawner.AnimalSpawnedCallback = function(animalTemplate, animalData) print("ð§ DEBUG: AnimalSpawnedCallback triggered for:", animalTemplate.Name) if animalTemplate then local traitsJson = HttpService:JSONEncode(validatedTraits) print("ð§ DEBUG: Applying traits to animal:", traitsJson) animalTemplate:SetAttribute("Traits", traitsJson) print("ð§ DEBUG: Traits attribute set. Current value:", animalTemplate:GetAttribute("Traits")) end if originalCallback then originalCallback(animalTemplate, animalData) end roadService.Spawner.AnimalSpawnedCallback = originalCallback end end local result = roadService.Spawner:SpawnSpecificAnimal(animalIndex, nil, mutation) if result == true then return true, "Animal queued for spawning" elseif type(result) == "userdata" then return true, result else return false, "Failed to spawn animal - spawner returned: " .. tostring(result) end end MessagingService:SubscribeAsync("GlobalRoadAnimalSpawn", function(message) local data = message.Data if data.FromServer == game.JobId then return end local success, result = pcall(function() return spawnRoadAnimal(data.AnimalName, data.Mutation, data.Traits) end) if not success then warn("Failed to spawn global road animal:", result) end end) return function(context, animalName, mutation, traits) local animalData = Animals[animalName] if not animalData then return "â Animal '" .. animalName .. "' not found" end if mutation and mutation ~= "" and mutation ~= "Default" and not Mutations[mutation] then return "â Mutation '" .. mutation .. "' not found" end if traits and traits ~= "" then local success, decodedTraits = pcall(function() return HttpService:JSONDecode(traits) end) if not success or type(decodedTraits) ~= "table" then return "â Invalid traits format. Use JSON array like '[\"Taco\", \"Fireworks\"]'" end for _, trait in ipairs(decodedTraits) do if not Traits[trait] then return "â Trait '" .. trait .. "' not found" end end end local cleanMutation = (mutation and mutation ~= "" and mutation ~= "Default") and mutation or nil local cleanTraits = (traits and traits ~= "") and traits or nil local success, result = spawnRoadAnimal(animalName, cleanMutation, cleanTraits) if not success then return "â Failed to spawn animal locally: " .. tostring(result) end local publishSuccess, publishError = pcall(function() MessagingService:PublishAsync("GlobalRoadAnimalSpawn", { AnimalName = animalName, Mutation = cleanMutation, Traits = cleanTraits, FromServer = game.JobId, Executor = context.Executor.Name }) end) if not publishSuccess then warn("Failed to publish global spawn message:", publishError) return "â ï¸ Animal spawned locally but failed to broadcast globally: " .. tostring(publishError) end local traitText = "" if cleanTraits then traitText = " with traits " .. cleanTraits end local mutationText = "" if cleanMutation then mutationText = " (" .. cleanMutation .. ")" end return "â Successfully spawned '" .. animalData.DisplayName .. mutationText .. traitText .. "' globally across all servers!" end